{
  "name": "growthhub-node",
  "version": "1.0.0",
  "description": "GrowthHub backend — Node.js + Express + MongoDB (Mongoose) version, ported from the Python + Flask + Postgres original.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.5.0",
    "node-telegram-bot-api": "^0.66.0"
  }
}
